# 001 计划主文档：任务定义与已定决策

最后更新：2026-09-10 22:40 CST（第 2 期完成后更新状态表）

## 分期执行状态

| 分期 | 状态 |
|---|---|
| 0 骨架与配置层 | ✅ 完成并验证（2026-09-10） |
| 1 web fallback | ✅ 完成并验证（含真实 key 下的 Kenari 优先路径，2026-09-10） |
| 2 REST 工具 | ✅ 完成并验证（20 个工具；真实调用 + 真实 attachment 落盘，2026-09-10；个别上游故障项见 002） |
| 3 目录计费 / 4 设置界面 / 5 LlmAdapter（可选）/ 6 打包文档 | ⬜ 下一任务（任务包见 002） |

每期完成后更新本表；设计全文见 `docs/plans/2026-09-10-dsh-kenari-plugin-design.md`。

## 任务

dsh bundle：Kenari 模型入会话、REST 能力成工具、web search/fetch 走 Kenari 优先带兜底。**红线：绝不修改 dsh 的任何代码或包。**

## 已定决策（不再讨论）

| 项 | 选型 |
|---|---|
| 范围 | 模型接入 + REST 工具，都做 |
| 交付形态 | dsh bundle |
| key | dsh 原生表单 + 凭据引用 `KENARI_API_KEY` |
| search / fetch 链 | Kenari 优先 → `deepseek-official` / 本地 `http` 兜底 |
| MCP | 不做主路径，README 提供为可选方案 |

## 未解除的未知项

1. **install scripts 拦截**（全局 dsh 的 `dsh-subprocess-local`/koffi/node-pty 等 5 包）：bash 工具/PTY/subagent 是否受影响未验证；第 1、2 期不依赖子进程，bash 异常时先查这里
2. **`llm-pi-ai` provider 行配置字段形状**：第 3 期前核对

## 关键事实速查（别处没有、易踩的）

- patch 覆盖**逐字段**：`config` 单键整体替换，`disabled` 独立键；同 config 内字段须写全；patch 行 `name` 不匹配目标行 → 整个 patch 被静默跳过
- web seam 无回退链；多 provider 未配 id → `WEB_PROVIDER_AMBIGUOUS`；`available()` 禁止网络请求，回退只能发生在调用失败后
- 兜底 provider 类（`DeepSeekSearchProvider` / `HttpFetchProvider`）可 import 实例化，**绝不注册**（id 冲突 → `WEB_DUPLICATE_PROVIDER`）
- Kenari base URL 两形状：[OI] `https://kenari.id/v1`，Anthropic 不带 `/v1`；错形状报 **405 非 404**
- 价格单位 micro-IDR per 1M tokens；免费模型 = `:free` 后缀或 `pricing.free`；`step-3-7-flash:free` 在 Rp 0 账户可用
- 分享页 key：balance/usage/quota 返 403，spending 工具正常；`web_search`/`web_fetch`/`x_search` 按次真实扣费，失败不计费

完整接口事实在知识库（`docs/knowledge-base/`）：dsh 侧 `dsh/dsh-source-verified.md`（权威性最高）等 4 篇 + `plugin-rules.md` 15 条硬约束；Kenari 侧 `kenari-plugin/kenari-api.md`。改代码前先读，不凭记忆推测。
