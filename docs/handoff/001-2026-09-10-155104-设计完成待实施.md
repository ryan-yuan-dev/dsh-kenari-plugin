# 交接文档：设计完成，待实施

文件：`001-2026-09-10-155104-设计完成待实施.md`
最后更新：2026-09-10 15:51:04 CST

> **执行状态（2026-09-10 更新）**：第 0 期已完成并验证。分期进度与未知项的最新状态见本文"分期执行状态"一节与 `docs/plans/2026-09-10-dsh-kenari-plugin-design.md` 第 4/5 节；接手开工读最新的接力主文档（当前为 `002-2026-09-10-161954-接力主文档.md`，已合并原 002/003/004）。

> 注（2026-09-10 拆分后）：知识库已按 dsh 侧 / Kenari 侧分目录，下文的 `docs/knowledge-base/*.md` 现位于 `docs/knowledge-base/dsh/`（dsh-* 与 plugin-rules.md 中 dsh 事实部分）与 `docs/knowledge-base/kenari-plugin/`（kenari-api.md）。

## 命名规则（硬规则）

交接文档命名格式：`NNN-yyyy-MM-dd-HHmmss-title.md`

- `NNN` 三位序号，从 `001` 递增，同目录按序号即时间顺序
- `yyyy-MM-dd-HHmmss` 为创建时刻（本地时区）
- `title` 为简短中文描述

新增交接文档时序号取当前最大值 +1。

## 当前状态

**设计阶段已完成；第 0 期已实施并验证（2026-09-10），第 1 期待开工。**

仓库现状（第 0 期后）：

```
dsh-kenari-plugin/
├── package.json / tsconfig.json / cordis.patch.yml / src/   # 第 0 期产出
├── docs/
│   ├── knowledge-base/     # dsh/（dsh 侧）与 kenari-plugin/（Kenari 侧）分目录
│   ├── plans/              # 设计文档（第 4 节含分期状态标记）
│   └── handoff/            # 001 计划主文档 + 002 接力主文档
└── vendor/deepseek-harness/  # 源码 submodule，tag dsh-v0.1.5-rc.1
```

## 分期执行状态

| 分期 | 状态 |
|---|---|
| 第 0 期 骨架与配置层 | ✅ 完成并验证（2026-09-10） |
| 第 1 期 web fallback | ⬜ 下一任务 |
| 第 2–6 期 | ⬜ 未开始 |

**本文件记录设计与计划**。每期完成后更新本节状态；接手开工所需的活动上下文（环境回路、上期沉淀、下期任务包）在接力主文档（当前 `002-*`），不在本文件重复维护。

## 任务是什么

做一个 dsh bundle，装上后：

1. Kenari 的模型可成为 dsh 的会话模型
2. Kenari 的 REST 能力成为 agent 工具（OCR、图像、音视频、embeddings 等）
3. 默认 web 不可用时自动回退到 Kenari 的 web search / fetch

**硬约束：不修改 dsh 的任何代码或包。** 详见 `docs/knowledge-base/plugin-rules.md` 规则 1。

## 接手时先读什么

1. `docs/handoff/` 下最新的**接力主文档**（当前 `002-*`）—— 状态、上期沉淀、下期任务包
2. `docs/knowledge-base/plugin-rules.md` —— 13 条规则，违反必须回退
3. `docs/plans/2026-09-10-dsh-kenari-plugin-design.md` —— 完整设计，含分期实施
4. 按任务选读知识库（分目录）：
   - `docs/knowledge-base/dsh/dsh-source-verified.md`（权威性最高，patch 语义 / seam 签名 / 依赖定版）
   - `docs/knowledge-base/dsh/dsh-web-seam.md` —— **本项目核心价值的依据**，web fallback 全靠它
   - `docs/knowledge-base/dsh/dsh-plugin-model.md` —— 插件怎么写、怎么打包、怎么装
   - `docs/knowledge-base/kenari-plugin/kenari-api.md` —— 查端点与 schema
   - `docs/knowledge-base/dsh/dsh-other-seams.md` —— 需要动 llm / settings / credentials 时读

## 已定的决策（不要重新讨论）

| 项 | 选型 | 决策人 |
|---|---|---|
| 范围 | 模型接入 + 附加服务工具，两者都做 | 用户 |
| 交付形态 | dsh bundle | 用户 |
| key 输入 | 复用 dsh 原生 provider 表单 + 凭据引用 `KENARI_API_KEY` | 用户 |
| search 链 | Kenari 优先 → 官方 `deepseek-official` 兜底 | 用户 |
| fetch | 开启（`0.1.5-rc.1` 默认已启用），Kenari 优先 → 本地 HTTP 兜底 | 用户 |
| MCP | 不作为主路径，仅 README 里的可选零代码方案 | AI 建议，用户认可 |

## 下一步：第 0 期（✅ 已完成，2026-09-10）

产出：`package.json`、`tsconfig.json`、`cordis.patch.yml`、`src/index.ts`（Config schema 7 字段全带默认值 + 空 apply）。

验证结果：

- [x] `dsh --profile web --dump-config` 出现 `# == dsh-kenari-plugin` 层
- [x] `dsh --profile web` 启动无报错

产物与实测摘录见接力主文档（`002-*`）第 3 节；后续每期任务包也在那里，本节不再逐期复述。

**本文件记录设计与计划，执行状态见"分期执行状态"一节。**

## 实施前需要实测确认的未知项

以下条目**已在实施阶段解除**，保留供追溯：

1. ~~**peer 依赖版本范围**~~ → 已定：dsh 子包精确 `0.1.5-rc.1`；`@deepseek-ai/cordis ^4.0.2`（npm 无 0.1.5-rc.1）；`@deepseek-ai/schemastery ^3.18.1-rc.1`
2. ~~**`ctx.settings.installSection()` 的完整签名**~~ → 已核对源码（`packages/settings/settings/src/index.ts:472-505`）：`installSection(owner, ns, schema, entry, hooks)`，hooks 含必需的 `setSource`/`onChange` 与可选 `validate`；ns 匹配 `/^[a-z][a-z0-9-]*$/`
3. **`llm-pi-ai` provider 行的确切配置字段形状**：**仍待确认**（第 3 期动模型目录前核对；已确认 `LlmAdapter` 唯一必须实现 `stream()`）
4. ~~**`--dump-config` 的实际输出格式**~~ → 已定：`# == <bundle>` 层标记 + YAML 行
5. ~~**`dsh-web-fetch-http` 的安全语义版本差异**~~ → 已定：`0.1.5-rc.1` 已实现 SSRF 防护
6. ~~**`disabled: false` 在 patch 中的写法**~~ → 已核对源码并实测通过：`disabled` 是 patch 行的独立合法键
7. **子进程能力是否受 install scripts 拦截影响**：**仍待验证**（第 1、2 期不依赖子进程）

### 已解除的未知项

**`@deepseek-ai/dsh-web-fetch-http` 是否导出可实例化的 provider 类** —— 已验证：**是**。

导出 `HttpFetchProvider` 类与 `LOCAL_FETCH_PROVIDER_ID` 常量（值为 `http`）。`0.1.5-rc.1` 的 `HttpFetchLimits` 为 5 个字段（`maxResponseBytes`、`maxBodyChars`、`timeoutMs`、`maxRedirects`、`userAgent`），旧版 `0.0.1-rc.5` 多一个 `maxUrlLength`。`available()` 恒为 `true`（匿名公开抓取，无凭据可查）。

所以 fetch 兜底可实现：直接实例化 `HttpFetchProvider`，**不注册**（避免与 `dsh-base` 已注册的 id 冲突），只持有实例调用其 `fetch()`。

同类验证结果：`dsh-web-search-deepseek` 导出 `DeepSeekSearchProvider`（id `deepseek-official`），`dsh-web-search-exa` 导出 `ExaSearchProvider`（id `exa`，**该包未随 dsh 安装**）。均可实例化复用。详见 `docs/knowledge-base/dsh-web-seam.md` 的"各 provider 包的导出与 id"。

## 已核实的关键事实速查

避免重新查文档：

- ~~**`dsh-base` 默认 `fetch: false` 且无 fetch provider**~~ → **已修正（0.1.5-rc.1 实测）**：`fetch: true` 且 `fetchProvider: http`，`web_fetch` 默认可用，`web-fetch-http` 行默认挂载。Kenari fetch 是"替换默认 provider"而非"从无到有"
- **`dsh-base` 已钉 `searchProvider: deepseek-official`** → 不会 AMBIGUOUS，但要接入自己的 provider 必须覆盖 `- id: web` 那一行
- **web seam 无回退链**，多 provider 未配 id 会报 `WEB_PROVIDER_AMBIGUOUS`，不是先到先得
- **`available()` 禁止发网络请求** → 回退只能在调用失败后发生
- ~~**patch 替换整行 config，不合并**~~ → **已修正（源码核对）**：patch 覆盖是**逐字段**的——`config` 作为单键整体替换，`disabled` 等是独立键；同一 config 内字段仍须写全；patch 行写 `name` 必须与目标行一致否则整个 patch 被跳过
- **`ctx.web` / `ctx.llm` / `ctx.settings` / `ctx.credentials` 是 seam；`ctx.tools` / `ctx.tokenMeter` 是 core**
- **三个 provider 类都可实例化复用**：`DeepSeekSearchProvider`(id `deepseek-official`)、`ExaSearchProvider`(id `exa`)、`HttpFetchProvider`(id `http`，`available()` 恒真)。**复用时不注册**，否则抛 `WEB_DUPLICATE_PROVIDER`
- **Kenari base URL 分两种形状**：`[OI]` 用 `https://kenari.id/v1`，Anthropic 用 `https://kenari.id`（不带 `/v1`）；形状错误返回 **405 而非 404**
- **Kenari 价格单位是 micro-IDR per 1M tokens**
- **免费模型**：id 加 `:free` 后缀，或 `pricing.free` 为真。`step-3-7-flash:free` 在 Rp 0 账户可用
- **分享页 key 会被拒 balance / usage / quota**（403），spending 工具仍可用
- **计费工具真实扣费**：`web_search`、`web_fetch`、`x_search` 按次计费，失败调用不计费

## 文档来源的可靠性提醒

用户最初给的两个页面**内容很薄，不是权威依据**：

- `kenari.id/en/docs/agents` —— 只有 MCP 工具功能表，**无参数、无配置 JSON、无传输类型**
- `deepseek-harness/.../guide/quickstart` —— 只是 Web UI 使用说明，**无插件内容**

权威来源是：

- dsh：npm 包的 `.d.ts` 与 `cordis.patch.yml`（**优先于文档**），以及 `/en/develop/`、`/en/reference/`
- Kenari：`https://kenari.id/openapi.json`，以及 `/en/docs/tools`（MCP 配置与传输类型在这里）

## 已知风险

见设计文档第 7 节。最需要注意的两个：

1. **web 走向被改变** —— 钉住 `searchProvider` 会让所有 web 搜索先走 Kenari。这是设计目标，但要在 README 说清，并提供 `fallbackEnabled: false` 退回
2. **兜底实例 id 冲突** —— 误注册会抛 `WEB_DUPLICATE_PROVIDER` 导致加载失败。代码里必须注释标注"不注册"

## 工作约定

- 计划写入 `docs/plans/`
- 交接写入 `docs/handoff/`，文件名遵循 `NNN-yyyy-MM-dd-HHmmss-title.md`
- 知识库写入 `docs/knowledge-base/`
- 接口事实必须沉淀到知识库，不要只留在对话里
- 修改插件前先读知识库，不要凭记忆推测 dsh 或 Kenari 的接口
