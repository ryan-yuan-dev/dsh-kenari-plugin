# 003 接力主文档：第 3 期开工包

创建：2026-09-10 22:27 CST（第 2 期完成后新建）

> **状态：本包已执行完毕（2026-09-10 23:59 CST）。** 第 3、4、6 期完成并验证，第 5 期（可选）明确不做；
> 第 2 期遗留 1–5 已收尾（TTS 是模型级故障、video_content 已实测、fallback ④ 已补测）。
> 实测事实已归入知识库（`dsh/dsh-source-verified.md` §13–14、`kenari-plugin/kenari-api.md` 第 3 期补充），
> 交付说明见仓库 README。按用户指示不再新建下一期接力文档。

**读法：001（计划主文档）给任务与决策，本文给"第 3 期怎么干"。知识库按需查，不预读。**

## 状态

第 0、1、2 期完成。第 3 期尚未开工。

第 2 期交付 `src/tools/` 8 个文件（`shared.ts` + docs/account/x-search/documents/media/data/count-tokens）共 20 个工具、`http.ts` 完整传输层、`index.ts` 装配。`lib/` 已编译，`git main` 干净。

## 环境回路（每期都用）

- 插件已 pnpm link 进 `~/.dsh/profiles/web`：改源码只跑 `pnpm build`，**别重新 add**；patch 改动即时生效
- 启动验证遇 3080 被占（用户实例）：`dsh --profile web --port 3099 --no-open`，不杀用户进程
- 装 dsh 子包必须带 `@0.1.5-rc.1`；pnpm 拦 rc 版本时往 `pnpm-workspace.yaml` 的 `minimumReleaseAgeExclude` 加条目
- peer 定版：dsh 子包精确 `0.1.5-rc.1`、`@deepseek-ai/cordis ^4.0.2`、`@deepseek-ai/schemastery ^3.18.1-rc.1`；第 2 期新增 `@deepseek-ai/dsh-attachment`、`@deepseek-ai/dsh-util-values`
- **key 隐私纪律（用户已确认）**：检查只用布尔 `[ -n "${(P)v}" ]`；看配置文件用 `sed 's/=.*/= ***/'` 脱敏；key 值永不进命令输出/日志/文档/上下文
- **`KENARI_API_KEY` 已配置**：写在 `~/.dsh/.env`（600），dsh 启动时由 `loadLayeredEnv` 以 user-env 层加载。`DEEPSEEK_API_KEY` 仍未配置

## 验证脚手架（第 2 期建好，继续用）

- **mock ctx**：`ctx.tools.register` 收工具、`ctx.web` 给两个 register 假函数，然后直接调 `tool.execute(args, execStub)`。**mock 环境读不到 `~/.dsh/.env`**，要先 `set -a; source ~/.dsh/.env; set +a`
- **真实 harness（更强，推荐）**：用 dsh 自己的服务组装，覆盖 register 断言 → registry 派发 → 参数校验 → render → 结果物化：

  ```js
  ctx.plugin(systemPromptPlugin)                     // dsh-system-prompt（tools 运行时 inject 它）
  ctx.plugin(toolsPlugin)                            // dsh-tools，provide ctx.tools
  ctx.plugin(attachmentPlugin, { dshHome: tmpdir })  // dsh-attachment-local，provide ctx.attachments
  ctx.provide('web', { registerSearchProvider(){}, registerFetchProvider(){} })
  ctx.plugin(kenari, { apiKeyEnv: 'KENARI_API_KEY' })
  await ctx.get('tools').execute({ callId, name, arguments: args, signal })
  ```
  这些包的真实路径在 `/opt/homebrew/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/`
- 计费类验证用最小调用：图像 1 张（gpt-image-2 约 Rp 175）、OCR 单页（Rp 75）、视频最短档（`gemini-omni-flash` 360p 4 秒约 Rp 1.4k）；**先看 `pricing_lines` 再决定**

## 第 2 期遗留（第 3 期顺带收尾）

1. **模型驱动调用未验（阻塞在模型接入）**：`~/.dsh/settings.yaml` 无 `llm-pi-ai` 配置节，dsh 当前没有会话模型，所以"Web UI 里模型点工具"要等第 3 期把 Kenari 模型接进 Settings → Models 后才能做。工具本身在 execute 层与真实 harness 层都已验证
2. **TTS 上游故障（Kenari 侧）**：`POST /v1/audio/speech` 对 gemini-3-1-flash-tts 与 kokoro-tts 均返回 400（最小请求体也一样），文案 `the model's provider rejected this request`。`kenari_speech` 未跑通，`kenari_transcribe` 因缺音频输入也没验。失败不计费。下期复测一次，仍失败就只在 README 标注
3. **视频只验到任务创建与状态轮询**：`kenari_video_content`（下载）未验；`kenari_music` 无模型可验（公开目录无 music 端点模型）；`kenari_moderate` 无模型（`?modality=moderation` 返回空）
4. **分享页 key 的 403 说明未实测**：手上只有普通 key，无法构造 403。代码路径已按 OpenAPI 的 `shared_key_not_allowed` 写
5. 第 1 期的 web fallback 场景④（无效 Kenari key → 回退）仍未实测；②③ 已随真实 key 生效

## 第 3 期：目录、计费与上下文

任务包（设计文档第 4 节第 3 期条目为完整版）：

- `catalog.ts`：`GET /v1/models` 同步 + TTL 缓存；`id` / `modalities.input` / `reasoning_options` / `context_length` / `tool_call` / `sunset_at` / `beta` / `pricing` / `pricing_lines` / `endpoints` 映射；`:free` 或 `pricing.free` 免费分组；模型别名
- 计费：单次扣费回显（工具已有 `cost_micro_idr`，补会话累计）、余额低阈值告警、402 `insufficient_balance` 可执行建议、预算封顶
- 上下文：`count_tokens` 接入 token 计量；`cached_tokens` 命中率
- 模型接入（零代码，但要落文档/预设）：`llm-pi-ai` 三种协议 base URL 区分 —— [OI]/Responses 用 `https://kenari.id/v1`，Anthropic 用 `https://kenari.id`（不带 /v1）；兼容预设 `supportsDeveloperRole: false`、`maxTokensField: max_tokens`；新账户默认 `step-3-7-flash:free`
- 完成后补：模型驱动下的工具调用验证（上面遗留 1）

**价格事实（实测，可直接用于展示）**：`pricing` 单位 `micro_idr_per_1m_tokens`，除以 `1_000_000` 得 IDR/1M；非 token 端点看 `pricing_lines`（图像 `unit: image`、语音 `unit: 1k_chars`、视频 `unit: second` 且按分辨率分档）。

## 完成一期后的固定动作

1. 更新 001 状态表与设计文档第 4 节标记
2. **新建下一期序号的接力主文档**（吸收本期仍有效事实 + 下期任务包，自足可开工）；本期文档保留为记录
3. `git commit`
