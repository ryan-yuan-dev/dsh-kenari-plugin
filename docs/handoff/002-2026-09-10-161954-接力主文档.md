# 002 接力主文档：下期开工包

最后更新：2026-09-10 21:30 CST（第 1 期完成后重写）

**读法：001（计划主文档）给任务与决策，本文给"下一期怎么干"。知识库按需查，不预读。**

## 状态

第 0、1 期完成。第 1 期交付 `src/errors.ts` / `http.ts` / `web/{search,fetch,fallback}.ts` + `index.ts` 装配。已验证：dump-config 钉层生效、启动无报错、mock ctx 装配三路径（组合/无兜底/全关）、无效 key → 401 → 回退兜底 + warn 日志。git `main` 干净、无 remote。

## 环境回路（每期都用）

- 插件已 pnpm link 进 `~/.dsh/profiles/web`：改源码只跑 `pnpm build`，**别重新 add**；patch 改动即时生效
- 启动验证遇 3080 被占（用户实例）：`dsh --profile web --port 3099 --no-open`，不杀用户进程
- 装 dsh 子包必须带 `@0.1.5-rc.1`；pnpm 拦 rc 版本时往 `pnpm-workspace.yaml` 的 `minimumReleaseAgeExclude` 加条目
- peer 定版：dsh 子包精确 `0.1.5-rc.1`、`@deepseek-ai/cordis ^4.0.2`、`@deepseek-ai/schemastery ^3.18.1-rc.1`

## 第 1 期遗留（第 2 期顺带收尾）

- **真实计费验证未做**：`KENARI_API_KEY` 未配置；需用户配 key 后验证 ②无 DEEPSEEK_KEY 走 Kenari ③配了仍走 Kenari ④无效 Kenari key 回退（冒烟已过，待真环境）。`web_search`/`web_fetch` 按次扣费，最小调用量
- Kenari 实现了 retry（http.ts 内联，429/408/5xx 指数退避），回退日志 format：`kenari-fallback: <方向> 失败后回退（<原因>，<耗时>ms）`

## 第 2 期：REST 工具

**写 7 个文件**（`src/tools/`）：`docs.ts`（kenari_search_docs 本地检索 /llms-full.txt + kenari_list_models）、`account.ts`（balance/usage/quota，403 = 分享页 key）、`x-search.ts`（过滤参数全带：handles ≤20 互斥、日期、图像/视频理解）、`documents.ts`（OCR，`reuse_id` 会话内复用防重复计费）、`media.ts`（图像/语音/音乐/视频全家）、`data.ts`（embeddings/rerank/moderations）、`count-tokens.ts`。

**统一骨架**：每个工具 `defineTool` + `ctx.tools.register`，`inject` 加 `'tools'`；复用 `http.ts` 的 `kenariPost`（自带超时/重试/key 解析）；`cost_micro_idr` 回显进 output。

**defineTool 关键签名**（源码实读 §6）：`parameters` 属性级 `required: true` 标注；`object` 型**必须** `additionalProperties: boolean`；`execute(args, exec)` 返回 `output.schema` 声明的值，`output.render(args, value) → ContentBlock[]`。

**验证**：Web UI 逐工具调用成功；`reuse_id` 二次不重复计费；handles 同传报参数错；分享页 key 403 有说明。

## 完成一期后的固定动作

1. 更新 001 状态表与设计文档第 4 节标记
2. **重写本文**（吸收上期仍有效事实，替换任务包为下期）
3. `git commit`
